import { SearchInput } from "./shared";
import { useState } from "react";
import { Markdown, experimental_SourceCode as SourceCode } from "@get-bb/plugin-sdk/app";
import { Switch } from "../../../components/ui/switch";
import { Button, Input, TextField, Choice, PageHead, TabBar, Panel, Field, Rows, Status, Empty, Collection } from "./shared";
import { type Job } from "./data";
export function RunsPage({ detail, open, back, notice, openJob }: {openJob:()=>void;detail:boolean;open:()=>void;back:()=>void;notice:(s:string)=>void}) {const[tab,setTab]=useState("Результат");return <div className="space-y-5">{detail&&<Button variant="ghost" size="sm" onClick={back}>← Запуски</Button>}<PageHead title={detail?"RUN-204 · Подготовка оффера":"Запуски"} description="Запуск — одна попытка работы сотрудника. У задачи может быть несколько запусков.">{detail&&<Button variant="outline" onClick={openJob}>Открыть задачу AG-102</Button>}</PageHead>{!detail?<Collection columns={["Запуск","Состояние","Задача","Длительность"]} rows={[{id:"RUN-204",open,name:<span><span className="block font-medium">RUN-204 · Анна</span><span className="text-xs text-muted-foreground">Подготовка оффера</span></span>,cells:["Результат передан","AG-102","3 мин 42 с · пример"]}]}/>:<><TabBar value={tab} onChange={setTab} tabs={["Результат","События","Профиль запуска","Диагностика"]}/>{tab==="Результат"&&<Panel title="Итог примера"><Markdown content={"Подготовлен **оффер v2**. Материал передан независимому редактору.\n\n- Уточнена аудитория.\n- Удалены неподтверждённые обещания.\n- Остаётся согласовать визуальную концепцию."}/></Panel>}{tab==="События"&&<Panel title="Пример журнала"><Rows rows={[["1. Работа начата",<span>Анна <span className="text-xs text-muted-foreground">(run.started)</span></span>],["2. Создан файл",<span>Оффер v2 <span className="text-xs text-muted-foreground">(artifact.created)</span></span>],["3. Результат передан",<span>На проверку <span className="text-xs text-muted-foreground">(result.delivered)</span></span>]]}/><p className="mt-3 text-xs text-muted-foreground">Для реального треда здесь будет штатный ThreadChat BB в режиме просмотра истории.</p></Panel>}{tab==="Профиль запуска"&&<Panel title="Неизменяемый снимок"><Rows rows={[["Сотрудник","Анна · версия 1"],["Навыки","copywriter, ru-text"],["MCP","Документы"],["Вход","Бриф v1"],["Результат","Оффер v2"]]}/></Panel>}{tab==="Диагностика"&&<Panel title="Состояние исполнения"><Rows rows={[["Тип записи","Демонстрация"],["Реальный threadId","Не создан"],["Расход","Нет реального измерения"]]}/><p className="mt-3 text-xs text-muted-foreground">Повтор станет доступен для реальных неудачных или отменённых попыток. Этот запуск — пример.</p></Panel>}</>}</div>;}
export interface AgencySettings {limit:string;paused:boolean;tz:string;days:string;}
export const seedSettings:AgencySettings={limit:"3",paused:false,tz:"Europe/Madrid",days:"30"};
export function SettingsPage({hosts,notice,reset,settings,setSettings}:{hosts:{id:string;name:string}[];notice:(s:string)=>void;reset:()=>void;settings:AgencySettings;setSettings:(s:AgencySettings)=>void}) {const[tab,setTab]=useState("Общие");const {limit,paused,tz,days}=settings;const setLimit=(limit:string)=>setSettings({...settings,limit:String(Math.max(1,Math.min(20,Number(limit)||1)))});const setPaused=(paused:boolean)=>setSettings({...settings,paused});const setTz=(tz:string)=>setSettings({...settings,tz});const setDays=(days:string)=>setSettings({...settings,days:String(Math.max(1,Math.min(365,Number(days)||1)))});return <div className="space-y-5"><PageHead title="Настройки" description="Общие ограничения, машины, подключения и восстановление."><Button onClick={()=>notice("Настройки изменены только в макете.")}>Сохранить настройки</Button></PageHead><TabBar value={tab} onChange={setTab} tabs={["Общие","Машины","Подключения","Хранение","Диагностика"]}/><div className="max-w-3xl space-y-4">{tab==="Общие"&&<Panel title="Работа агентства"><div className="space-y-4"><TextField label="Общий лимит параллельных задач" type="number" value={limit} onChange={setLimit}/><Field label="Часовой пояс агентства"><Choice label="Часовой пояс агентства" value={tz} onChange={setTz} options={["Europe/Madrid","Europe/Moscow","UTC"]}/></Field><div className="flex items-center justify-between"><span className="text-sm">Приостановить новые назначения</span><Switch aria-label="Приостановить назначения" checked={paused} onCheckedChange={setPaused}/></div></div></Panel>}{tab==="Машины"&&<Panel title="Машины из BB">{hosts.length?<Rows rows={hosts.map(h=>[h.name,"Зарегистрирована в BB"] as [string,string])}/>:<Empty title="Каталог не загружен" description="Проверьте соединение с BB."/>}<p className="text-xs text-muted-foreground">Регистрация и обслуживание машин остаются в штатных настройках BB.</p></Panel>}{tab==="Подключения"&&<Panel title="Каталог доступов"><Rows rows={[["MCP","Разрешаются в профиле сотрудника"],["Skills","Выбираются отдельно от инструментов"],["Секреты","Планируется серверное хранение и выдача по разрешённым ссылкам"],["Проверка изоляции","Ещё не реализована"]]}/></Panel>}{tab==="Хранение"&&<Panel title="История и резервирование"><TextField label="Срок хранения подробных журналов, дней" type="number" value={days} onChange={setDays}/><div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" onClick={()=>notice("Экспорт будет включать профили и правила без секретов. В прототипе файл не создаётся.")}>Экспорт конфигурации</Button><Button variant="outline" onClick={()=>notice("Импорт будет показывать изменения до применения; автоматизации выключены по умолчанию.")}>Импорт конфигурации</Button></div></Panel>}{tab==="Диагностика"&&<Panel title="Режим прототипа"><Rows rows={[["Данные интерфейса","Демонстрационные, в памяти страницы"],["Выбор CLI и модели","Штатный каталог BB"],["Исполнение","Недоступно"],["События и cron","Только предпросмотр"]]}/><Button variant="outline" onClick={reset}>Сбросить пример</Button></Panel>}</div></div>;}
