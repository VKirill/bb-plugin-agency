import { useState } from "react";
import { experimental_ProviderModelPicker as ProviderModelPicker, experimental_PermissionModePicker as PermissionModePicker, experimental_NewThreadComposer as NewThreadComposer } from "@get-bb/plugin-sdk/app";
import { Switch } from "../../../components/ui/switch";
import { type Agent, type Group, type Job, skillOptions, mcpOptions } from "./data";
import { Button, Input, TextField, Field, Choice, PageHead, TabBar, Panel, Checks, Rows, Empty } from "./shared";
import { JobsPage } from "./jobs";

export function AgentsPage({ agents, open, create }: {agents:Agent[];open:(id:string)=>void;create:()=>void}) {
 const [q,setQ]=useState("");const [dep,setDep]=useState("all");
 return <div className="space-y-5"><PageHead title="Сотрудники" description="Роли, инструкции и возможности каждого агента."><Button onClick={create}>Создать сотрудника</Button></PageHead><div className="flex flex-wrap gap-2"><Input className="max-w-sm" aria-label="Поиск сотрудников" placeholder="Найти сотрудника…" value={q} onChange={e=>setQ(e.target.value)}/><div className="w-48"><Choice label="Отдел сотрудника" value={dep} onChange={setDep} options={[{value:"all",label:"Все отделы"},"Маркетинг","Редакция","Дизайн","Исследования"]}/></div></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{agents.filter(a=>(a.name+" "+a.role).toLowerCase().includes(q.toLowerCase())&&(dep==="all"||a.department===dep)).map(a=><button key={a.id} onClick={()=>open(a.id)} className="rounded-lg border border-border bg-card p-4 text-left hover:bg-muted/40 focus-visible:outline focus-visible:outline-ring"><div className="mb-3 flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-full bg-muted text-sm font-semibold">{a.name.slice(0,1)}</span><div><h2 className="font-medium">{a.name}</h2><p className="text-xs text-muted-foreground">{a.role}</p></div></div><p className="text-sm">{a.department}</p><p className="mt-2 text-xs text-muted-foreground">Навыки: {a.skills.length} · MCP: {a.mcps.length}</p><p className="mt-3 border-t border-border pt-3 text-xs">{a.enabled?"Доступен для назначения в примере":"Приостановлен в примере"}</p></button>)}</div></div>;
}
export function AgentDetail({ agent, update, hosts, back, notice }: {agent:Agent;update:(a:Agent)=>void;hosts:{id:string;name:string}[];back:()=>void;notice:(s:string)=>void}) {
 const [tab,setTab]=useState("Профиль");const [q,setQ]=useState("");const [shell,setShell]=useState(false);const [delegate,setDelegate]=useState(false);
 const hostId=agent.hostId||hosts.find(h=>/mini/i.test(h.name))?.id||hosts[0]?.id;const routing=hostId?{kind:"host" as const,hostId}:undefined;
 return <div className="space-y-5"><Button variant="ghost" size="sm" onClick={back}>← Сотрудники</Button><PageHead title={agent.name} description={agent.role}><Button variant="outline" onClick={()=>update({...agent,enabled:!agent.enabled})}>{agent.enabled?"Приостановить":"Активировать"}</Button><Button onClick={()=>notice("Профиль сохранён в текущем прототипе. Настройки исполнения не применяются.")}>Сохранить профиль</Button></PageHead><TabBar value={tab} onChange={v=>{setTab(v);setQ("");}} tabs={["Профиль","Инструкции","Исполнение","Навыки","MCP и доступ","Проверка","История"]}/>
 <div className="max-w-4xl space-y-4">
 {tab==="Профиль"&&<Panel title="Сотрудник"><div className="grid gap-4 md:grid-cols-2"><TextField label="Имя" value={agent.name} onChange={name=>update({...agent,name})}/><TextField label="Роль" value={agent.role} onChange={role=>update({...agent,role})}/><Field label="Отдел"><Choice label="Отдел" value={agent.department} onChange={department=>update({...agent,department})} options={["Маркетинг","Редакция","Дизайн","Исследования"]}/></Field><Field label="Статус"><Choice label="Статус сотрудника" value={agent.enabled?"active":"paused"} onChange={s=>update({...agent,enabled:s==="active"})} options={[{value:"active",label:"Доступен"},{value:"paused",label:"Приостановлен"}]}/></Field></div></Panel>}
 {tab==="Инструкции"&&<><Panel title="Обязанности и порядок работы"><TextField label="Инструкции сотрудника" multiline value={agent.instructions} onChange={instructions=>update({...agent,instructions})}/></Panel><Panel title="Контекст на каждом запуске"><Rows rows={[["1. Проект","Факты, правила и принятые решения"],["2. Отдел","Процесс и критерии приёмки"],["3. Сотрудник","Эти инструкции и выбранные навыки"],["4. Задача","Поручение и версии материалов"]]}/></Panel></>}
 {tab==="Исполнение"&&<><Panel title="CLI и модель"><div className="space-y-5"><Field label="Машина" hint="Каталог машин считывается из BB. Выбор влияет только на черновик профиля.">{hosts.length?<Choice label="Машина сотрудника" value={hostId||""} onChange={hostId=>update({...agent,hostId})} options={hosts.map(h=>({value:h.id,label:h.name}))}/>:<p className="text-sm">Каталог машин пока недоступен.</p>}</Field><Field label="CLI / модель / reasoning / service tier" hint="Штатный выбор BB: возможности и модели берутся из актуального каталога."><ProviderModelPicker value={agent.selection} onChange={selection=>update({...agent,selection})} routing={routing} allowProviderChange align="start"/></Field><Field label="Режим разрешений"><PermissionModePicker providerId={agent.selection.providerId} value={agent.permission} onChange={permission=>update({...agent,permission})} routing={routing} align="start"/></Field><TextField label="Одновременных задач" type="number" value={String(agent.concurrency)} onChange={v=>update({...agent,concurrency:Math.max(1,Math.min(8,Number(v)||1))})}/></div></Panel><Panel title="Рабочая папка"><p className="text-sm">Наследуется от проекта задачи. При передаче между отделами проект и рабочая машина сохраняются.</p><p className="mt-2 text-xs text-muted-foreground">Проверка совместимости и изоляции этого CLI ещё не выполнена.</p></Panel></>}
 {tab==="Навыки"&&<><PageHead title="Разрешённые навыки" description={`Выбрано: ${agent.skills.length}. Демонстрационный каталог; реальная фильтрация ещё не подключена.`}><Button variant="outline" onClick={()=>update({...agent,skills:[]})}>Снять все</Button></PageHead><Input aria-label="Поиск навыков" placeholder="Найти навык…" value={q} onChange={e=>setQ(e.target.value)}/><Checks options={skillOptions.filter(s=>s.includes(q.toLowerCase()))} selected={agent.skills} onChange={skills=>update({...agent,skills})}/></>}
 {tab==="MCP и доступ"&&<><PageHead title="Подключения MCP" description="Выбор в макете не выдаёт и не отзывает реальные доступы."/><Checks options={mcpOptions} selected={agent.mcps} onChange={mcps=>update({...agent,mcps})}/><Panel title="Дополнительные возможности"><div className="flex items-center justify-between py-3"><span className="text-sm">Команды терминала</span><Switch aria-label="Команды терминала" checked={shell} onCheckedChange={setShell}/></div><div className="flex items-center justify-between py-3"><span className="text-sm">Передача работы другим сотрудникам</span><Switch aria-label="Делегирование" checked={delegate} onCheckedChange={setDelegate}/></div><p className="text-xs text-muted-foreground">Доступ к файлам, сети и административным командам проверяется отдельно от видимости MCP.</p></Panel></>}
 {tab==="Проверка"&&<><Panel title="Что получит сотрудник"><Rows rows={[["Навыки",agent.skills.join(", ")||"Не выбраны"],["MCP",agent.mcps.join(", ")||"Не выбраны"],["CLI",agent.selection.providerId],["Модель",agent.selection.model||"Выбирается в каталоге BB"],["Изоляция","Не проверена — реальный запуск закрыт"]]}/><Button variant="outline" onClick={()=>notice("Предпросмотр обновлён. Проверка реального процесса будет доступна после этапа изоляции.")}>Проверить конфигурацию</Button></Panel><Empty title="Тестовое поручение пока недоступно" description="Здесь появится пробный запуск с просмотром фактически переданных навыков и инструментов."/></>}
 {tab==="История"&&<Panel title="Версии профиля"><Rows rows={[["Черновик","Изменения текущего прототипа"],["Версия 1","Пример исходного профиля"],["Исполнения","Будут привязаны к неизменяемой версии"]]}/></Panel>}
 </div></div>;
}
export function GroupsPage({ groups, kind, open, create }: {groups:Group[];kind:"projects"|"departments";open:(id:string)=>void;create:()=>void}) {return <div className="space-y-5"><PageHead title={kind==="projects"?"Проекты":"Отделы"} description={kind==="projects"?"Бизнес-контекст и подключённые команды.":"Повторяемые процессы, руководители и сотрудники."}><Button onClick={create}>{kind==="projects"?"Создать проект":"Создать отдел"}</Button></PageHead><div className="grid gap-3 lg:grid-cols-2">{groups.map(g=><button key={g.id} onClick={()=>open(g.id)} className="rounded-lg border border-border bg-card p-5 text-left hover:bg-muted/30"><h2 className="font-medium">{g.name}</h2><p className="mt-1 text-sm text-muted-foreground">{g.description}</p><div className="mt-4 flex justify-between text-xs"><span>Руководитель: {g.lead}</span><span>{g.members.length} {kind==="projects"?"отдела":"сотрудника"}</span></div></button>)}</div></div>;}
export function GroupDetail({ group, kind, update, jobs, setJobs, openJob, back, notice, people }: {group:Group;kind:"projects"|"departments";update:(g:Group)=>void;jobs:Job[];setJobs:(j:Job[])=>void;openJob:(id:string)=>void;back:()=>void;notice:(s:string)=>void;people:string[]}) {
 const [tab,setTab]=useState("Обзор");const [returnLimit,setReturnLimit]=useState("2");const isProject=kind==="projects";
 return <div className="space-y-5"><Button variant="ghost" size="sm" onClick={back}>← {isProject?"Проекты":"Отделы"}</Button><PageHead title={group.name} description={group.description}><Button variant="outline" onClick={()=>update({...group,enabled:!group.enabled})}>{group.enabled?"Пауза":"Продолжить"}</Button><Button onClick={()=>notice("Настройки сохранены в текущем прототипе.")}>Сохранить</Button></PageHead><TabBar value={tab} onChange={setTab} tabs={["Обзор","Задачи",isProject?"Отделы":"Сотрудники","Правила","Контекст","Автопилот",...(isProject?["Менеджер"]:[])]}/>
 {tab==="Обзор"&&<div className="grid gap-4 lg:grid-cols-2"><Panel title="Описание"><div className="space-y-4"><TextField label="Название" value={group.name} onChange={name=>update({...group,name})}/><TextField label="Назначение" multiline value={group.description} onChange={description=>update({...group,description})}/></div></Panel><Panel title="Ответственность"><Rows rows={[["Руководитель",group.lead],["Состояние",group.enabled?"Доступен в примере":"На паузе"],["Результат","Материалы и независимая проверка"]]}/><Button variant="outline" onClick={()=>setTab("Задачи")}>Открыть очередь</Button></Panel></div>}
 {tab==="Задачи"&&<JobsPage jobs={jobs} setJobs={setJobs} open={openJob} {...(isProject?{project:group.name}:{department:group.name})}/>}
 {(tab==="Сотрудники"||tab==="Отделы")&&<div className="max-w-3xl space-y-4"><Field label="Руководитель"><Choice label="Руководитель" value={group.lead} onChange={lead=>update({...group,lead})} options={["Мария","Марк","Илья","София"]}/></Field><Checks options={people} selected={group.members} onChange={members=>update({...group,members})}/></div>}
 {tab==="Правила"&&<div className="max-w-3xl space-y-4"><TextField label="Порядок работы и критерии приёмки" multiline value={group.instructions} onChange={instructions=>update({...group,instructions})}/><TextField label="Максимум возвратов на доработку" type="number" value={returnLimit} onChange={setReturnLimit}/><Panel title="Передача между отделами"><p className="text-sm">Заявка сохраняет проект, входные материалы, срок и ответственного за общий результат. Готовность дочерней задачи уведомляет руководителя.</p></Panel></div>}
 {tab==="Контекст"&&<Panel title={isProject?"Канон проекта":"Материалы отдела"}><Rows rows={[["Правила","AGENTS.md · пример привязки"],["Материалы","Позиционирование и критерии приёмки"],["Источник","Рабочая папка проекта задачи"],["Память","Только подтверждённые факты с источниками"]]}/><Button variant="outline" onClick={()=>notice("В полном продукте здесь выбирается источник через файловые средства BB. Макет не меняет привязки проекта.")}>Подключить материалы</Button></Panel>}
 {tab==="Менеджер"&&<Panel title="Поручение менеджеру проекта"><p className="mb-4 text-sm text-muted-foreground">Предпросмотр штатного редактора BB. Отправка здесь показывает подтверждение макета и не создаёт чат.</p><NewThreadComposer layout="document" draftKey={`agency-prototype-${group.id}`} placeholder="Какой результат нужен проекту?" onSubmit={async()=>{notice("Поручение показано в прототипе. Реальный чат и работники не созданы.");}}/></Panel>}
 {tab==="Автопилот"&&<Panel title="Когда включается руководитель"><Rows rows={[["Новое поручение","Определяет состав и следующий шаг"],["Результат принят","Продолжает зависимые этапы"],["Нужен выбор","Обращается к владельцу"],["Режим прототипа","События не запускают агентов"]]}/><Button variant="outline" onClick={()=>notice("Пробное событие показало бы маршрут: руководитель → исполнитель → проверяющий. Событие не отправлено.")}>Предпросмотр маршрута</Button></Panel>}
 </div>;
}
